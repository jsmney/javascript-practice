/*
 * Complete the function below.
 */
function greeting(name) {
    let greet = "Hello";
    if (name) {
        greet += ", " + name;
    }
    greet += "!";
    // Don't forget to return your output!
    return greet;
}

